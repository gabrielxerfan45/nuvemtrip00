:root {
  --primary: #e3f0ff;
  --accent: #90caf9;
  --text: #222;
  --white: #fff;
  --shadow: 0 4px 24px rgba(144,202,249,0.12);
}

* {
  box-sizing: border-box;
}

html {
  font-family: 'Inter', sans-serif;
  background: var(--primary);
  color: var(--text);
  scroll-behavior: smooth;
}

body {
  margin: 0;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

header {
  background: var(--white);
  box-shadow: var(--shadow);
  padding: 1.2rem 2rem;
  border-radius: 0 0 24px 24px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.logo {
  display: flex;
  align-items: center;
  gap: 0.7rem;
  font-weight: bold;
  color: var(--accent);
  font-size: 1.3rem;
}

nav a {
  color: var(--text);
  text-decoration: none;
  margin-left: 2rem;
  font-weight: 500;
  transition: color 0.2s;
}

nav a:hover {
  color: var(--accent);
}

main {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
  padding-top: 2rem;
}

.hero {
  background: var(--white);
  padding: 2rem 2rem 1rem 2rem;
  border-radius: 24px;
  box-shadow: var(--shadow);
  text-align: center;
  max-width: 450px;
  margin-bottom: 2rem;
}

.hero h1 {
  color: var(--accent);
  margin-bottom: 2rem;
  font-size: 1.7rem;
}

.search-form {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.search-form input,
.search-form button {
  padding: 0.8rem 1rem;
  border: none;
  border-radius: 12px;
  font-size: 1rem;
}

.search-form input {
  background: #f7fbff;
  color: var(--text);
  border: 1px solid #cbe5fc;
}

.search-form input:focus {
  outline: 2px solid var(--accent);
}

.search-form button {
  background: var(--accent);
  color: var(--white);
  font-weight: bold;
  cursor: pointer;
  transition: background 0.2s, transform 0.2s;
}

.search-form button:hover {
  background: #64b5f6;
  transform: scale(1.05);
}

.results {
  width: 100%;
  max-width: 540px;
  background: var(--white);
  border-radius: 24px;
  box-shadow: var(--shadow);
  padding: 2rem;
  min-height: 80px;
  margin-bottom: 2rem;
  text-align: left;
  font-size: 1.05rem;
  color: #555;
}

footer {
  background: var(--white);
  text-align: center;
  padding: 1rem 0;
  box-shadow: 0 -2px 10px rgba(144,202,249,0.10);
  border-radius: 24px 24px 0 0;
  font-size: 0.95rem;
  color: #666;
}

.toast {
  visibility: hidden;
  min-width: 220px;
  background: var(--accent);
  color: var(--white);
  text-align: center;
  border-radius: 12px;
  padding: 0.8rem 1.2rem;
  position: fixed;
  left: 50%;
  bottom: 40px;
  transform: translateX(-50%);
  z-index: 100;
  font-size: 1rem;
  opacity: 0;
  transition: opacity 0.4s, visibility 0.4s;
}

.toast.show {
  visibility: visible;
  opacity: 1;
}

/* Responsivo */
@media (max-width: 600px) {
  header, .hero, .results, footer {
    border-radius: 0;
    padding: 1rem;
  }
  .hero, .results {
    max-width: 98vw;
  }
  nav a {
    margin-left: 1rem;
  }
}
